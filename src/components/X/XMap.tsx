import * as React from 'react';
// import * as ReactMapboxGl from 'react-mapbox-gl';
// import { canUseDOM } from '../../utils/environment';
// import * as MapBoxGl from 'mapbox-gl';
// let MapBox = (canUseDOM ? ReactMapboxGl.default({
//     accessToken: 'pk.eyJ1Ijoic3RldmUta2l0ZSIsImEiOiJjamNlbnR2cGswdnozMzNuemxzMHNlN200In0.WHk4oWuFM4zOGBPwju74sw'
// }) : null);

// let geoCoder = MapBoxGl

export function XMap(props: { width?: number | string, height?: number | string, center?: { longitude: number, latitude: number } | string }) {
    // if (MapBox) {
    //     return (
    //         <MapBox style="mapbox://styles/mapbox/streets-v9"
    //             containerStyle={{ width: props.width, height: props.height }}
    //             center={props.center ? [props.center.latitude, props.center.longitude] : undefined}
    //             zoom={[16]}
    //         />
    //     );
    // } else {
    //     return null;
    // }
}
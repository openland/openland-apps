import * as React from 'react';
import { View } from 'react-native';

export class ZListItemFooter extends React.PureComponent {
    render() {
        return (
            <View height={56} />
        );
    }
}
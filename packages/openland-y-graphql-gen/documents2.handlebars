{ 
  "operations": [
{{#each operations }}
    { "name": "{{ name }}{{ toPascalCase operationType }}", "type": "{{#ifEq operationType 'mutation' }}mutation{{else}}query{{/ifEq}}", "hasVariables": {{hasVariables}} }
{{#unless @last}},{{/unless}}
{{/each}}
  ]
}
import * as React from 'react';
import { PView } from 'openland-pegasus/PView';

export const PowerupSample = React.memo(() => {
    return (
        <PView width={'100%'} height={100} backgroundColor="red">
            {}
        </PView>
    );
});
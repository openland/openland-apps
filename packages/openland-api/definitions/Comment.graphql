fragment CommentEntryFragment on CommentEntry {
    id
    deleted
    comment: betaComment {
        ...FullMessage
    }
    parentComment {
        id
        comment: betaComment {
            id
            message
        }
    }
    childComments {
        id
    }
}

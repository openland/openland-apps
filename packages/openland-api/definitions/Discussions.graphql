query Hubs {
    hubs {
        id
        title
        shortname
        type
        owner {
            id
            firstName
            lastName
        }
    }
}

query Hub($id: ID!) {
    hub(id: $id) {
        id
        title
        shortname
        type
        owner {
            id
            firstName
            lastName
        }
    }
}
query FeedChannelWriters($after: ID, $first: Int!, $id: ID!) {
  writers: alphaFeedChannelAdmins(after: $after, first: $first, id: $id) {
    __typename
    cursor
    items {
      __typename
      role
      user {
        __typename
        ...UserShort
      }
    }
  }
}
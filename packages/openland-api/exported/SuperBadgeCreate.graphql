mutation SuperBadgeCreate($name: String!, $userId: ID!) {
  superBadgeCreate(name: $name, userId: $userId) {
    __typename
    ... on User {
      badges {
        __typename
        ...UserBadge
      }
      id
      primaryBadge {
        __typename
        ...UserBadge
      }
    }
  }
}
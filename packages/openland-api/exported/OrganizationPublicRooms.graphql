query OrganizationPublicRooms($after: ID, $first: Int!, $organizationId: ID!) {
  organizationPublicRooms(after: $after, first: $first, id: $organizationId) {
    __typename
    cursor
    items {
      __typename
      ...SharedRoomView
    }
  }
}
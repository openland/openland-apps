mutation SuperBadgeVerify($badgeId: ID!, $userId: ID!) {
  superBadgeVerify(badgeId: $badgeId, userId: $userId) {
    __typename
    ... on User {
      badges {
        __typename
        ...UserBadge
      }
      id
      primaryBadge {
        __typename
        ...UserBadge
      }
    }
  }
}
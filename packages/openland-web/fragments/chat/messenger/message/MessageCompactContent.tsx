import * as React from 'react';
import { DataSourceWebMessageItem } from '../data/WebMessageItemDataSource';

export const MessageComponent = React.memo((props: { message: DataSourceWebMessageItem }) => {

    return null;
});

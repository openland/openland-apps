import * as React from 'react';

export const DiscussionEditorFragment = React.memo((props: { id: string }) => {
    // const unicorn = useUnicorn();
    // const { messageId, commentId } = unicorn.query;
    return null;
});